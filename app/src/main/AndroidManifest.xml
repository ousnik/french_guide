<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.french">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.android.french.PhrasesActivity"
            android:label="@string/category_phrases"/>
        <activity android:name="com.example.android.french.ColorsActivity"
            android:label="@string/category_colors"/>
        <activity android:name="com.example.android.french.FamilyActivity"
            android:label="@string/category_family"/>
        <activity android:name="com.example.android.french.NumbersActivity"
            android:label="@string/category_numbers"/>
        <activity android:name="com.example.android.french.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>